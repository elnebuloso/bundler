packages:
    foo:
        to: build
        version: datetime
        include:
            - public/.*
            - src/.*
            - vendor/.*
        exclude:
            - ./public/js/README.md
    bar:
        to: build
        version: file
        include:
            - resources/*
            - src/*
            - vendor/*
        exclude:
    baz:
        to: build
        version: file
        include:
            - bin/*
            - resources/*
            - src/*
            - vendor/*
        exclude: