# path from which the files are collected
folder: .

# path where the files are copied to
target: ./bundler

# subfolder under target, if empty, no subfolder
# $DATETIME
# $VERSION
# $ARGUMENT
subfolder:

# define include / exclude pattern
include:
  - src/.*
  - vendor/.*
exclude:
  - ^.+README.md